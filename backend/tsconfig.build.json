{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    /* Production build — emit JS, no ts-node needed */
    "noEmit": false,
    "skipLibCheck": true
  },
  "exclude": [
    "node_modules",
    "dist",
    "**/*.spec.ts",
    "**/*.test.ts"
  ]
}
